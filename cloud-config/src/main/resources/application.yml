server:
  port: 8081
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/xiaozhaoosc/spring-boot-learn.git
          search-paths: cloud-config/cloud-config-repo
      label: master
